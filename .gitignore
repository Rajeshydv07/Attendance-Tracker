# Logs and Credentials
email_logs.json
action_logs.json
staff_credentials.json

# Python Cache
__pycache__/
*.pyc

# Streamlit config (optional, but good to keep tracked if customized)
#.streamlit/config.toml
